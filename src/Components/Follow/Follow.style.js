import styled from 'styled-components';
import {View, Text} from 'native-base';

export const FollowItemContainer = styled(View)`
  border: solid 2px papayawhip;
`;

export const FollowItemText = styled(Text)`
  text-align: center;
  font-size: 25;
`;
